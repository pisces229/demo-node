# Lefthook

```bash
# install
pnpm add -D lefthook
# install hooks
pnpm lefthook install
# list hooks
ls .git/hooks
# run pre-commit hook
pnpm lefthook run pre-commit
# uninstall hooks
pnpm lefthook uninstall
```
